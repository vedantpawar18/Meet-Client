import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: { primary: { main: '#9BEC00' }, mode: 'light' },
  typography: { button: { textTransform: 'none' } }
});
export default theme;
